mixin productCard(product = {})
  .card
    img(src=`${product.imageUrls.sm}`)
    .cardText
      p #{product.brand} #{product.description}
    .cardPrice
      p $#{product.networkPrice}
    a(href=`/${product.pricing.productId}`)
      button(type="button") VIEW MORE